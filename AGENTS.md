# AGENTS.md

Agent rules for this project live in [`.cursor/rules/`](.cursor/rules/):
- `architecture.mdc` — architectural contracts, invariants, active stubs, data locations
- `workflow.mdc` — commands, testing requirements, commit conventions
- `unit-standards.mdc` — energy/power/price unit standards (applied to `src/**/*.ts`)
